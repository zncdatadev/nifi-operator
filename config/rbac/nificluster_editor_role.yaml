# This rule is not used by the project nifi-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the nifi.kubedoop.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: nifi-operator
    app.kubernetes.io/managed-by: kustomize
  name: nificluster-editor-role
rules:
- apiGroups:
  - nifi.kubedoop.dev
  resources:
  - nificlusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - nifi.kubedoop.dev
  resources:
  - nificlusters/status
  verbs:
  - get
